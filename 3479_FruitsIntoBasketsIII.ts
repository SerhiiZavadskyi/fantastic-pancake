function numOfUnplacedFruits(fruits: number[], baskets: number[]): number {
	let unplaced = 0;
	const remainsBaskets: number[] = [];

	for (let i = 0; i < fruits.length; i++) {}

	return unplaced;
}

console.log(numOfUnplacedFruits([4, 2, 5], [3, 5, 4]));
